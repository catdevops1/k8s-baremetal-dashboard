apiVersion: apps/v1
kind: Deployment
metadata:
  name: netdata-parent-fixed
  namespace: netdata
  labels:
    app: netdata
    component: parent
    release: netdata
    role: parent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: netdata
      component: parent
      release: netdata
      role: parent
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: netdata
        component: parent
        release: netdata
        role: parent
    spec:
      nodeSelector:
        node-role.kubernetes.io/control-plane: ""
      tolerations:
        - effect: NoSchedule
          key: node-role.kubernetes.io/master
          operator: Exists
        - effect: NoSchedule
          key: node-role.kubernetes.io/control-plane
          operator: Exists
      initContainers:
        - name: inject-streaming-key
          image: busybox:1.36
          command: ['sh', '-c']
          args:
            - |
              KEY=$(cat /secret/API_KEY)
              cp /config-template/netdata.conf /config/netdata.conf
              cp /config-template/stream.conf /config/stream.conf
              sed -i "s/__STREAMING_API_KEY__/$KEY/g" /config/netdata.conf
              sed -i "s/__STREAMING_API_KEY__/$KEY/g" /config/stream.conf
              echo "Streaming key injected successfully"
          volumeMounts:
            - name: config-template
              mountPath: /config-template
            - name: config-rendered
              mountPath: /config
            - name: streaming-key
              mountPath: /secret
              readOnly: true
      containers:
        - name: netdata
          image: netdata/netdata:v1.44.3
          imagePullPolicy: IfNotPresent
          env:
            - name: NETDATA_MODE
              value: parent
            - name: NETDATA_STREAM_ENABLED
              value: "true"
            - name: NETDATA_CLAIM_TOKEN
              value: ""
            - name: NETDATA_CLAIM_ROOMS
              value: ""
            - name: NETDATA_CLAIM_URL
              value: ""
          ports:
            - containerPort: 19999
              name: http
              protocol: TCP
            - containerPort: 8125
              name: statsd
              protocol: UDP
          livenessProbe:
            httpGet:
              path: /api/v1/info
              port: 19999
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/info
              port: 19999
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            capabilities:
              add:
                - SYS_PTRACE
              drop:
                - ALL
            runAsNonRoot: false
            runAsUser: 201
          volumeMounts:
            - mountPath: /etc/netdata/netdata.conf
              name: config-rendered
              subPath: netdata.conf
            - mountPath: /etc/netdata/stream.conf
              name: config-rendered
              subPath: stream.conf
            - mountPath: /var/lib/netdata
              name: netdata-lib
            - mountPath: /var/cache/netdata
              name: netdata-cache
            - mountPath: /host/proc
              name: proc
              readOnly: true
            - mountPath: /host/sys
              name: sys
              readOnly: true
            - mountPath: /host/run
              name: run
              readOnly: true
            - mountPath: /var/run/docker.sock
              name: docker-sock
      volumes:
        - name: config-template
          configMap:
            name: netdata-parent-config
        - name: config-rendered
          emptyDir: {}
        - name: streaming-key
          secret:
            secretName: netdata-streaming-key
        - name: netdata-lib
          emptyDir: {}
        - name: netdata-cache
          emptyDir: {}
        - name: proc
          hostPath:
            path: /proc
        - name: sys
          hostPath:
            path: /sys
        - name: run
          hostPath:
            path: /run
        - name: docker-sock
          hostPath:
            path: /var/run/docker.sock
